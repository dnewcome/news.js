refactor things so that all user account stuff is 
under some folder so that we can pull it in really fast

news app for node.js

# Installing:

install node
install npm
npm install

# package dependencies:

express
node-mysql
jade

TODO:
pagination
x redo icon
x user creation
x comment threads
- maybe redo things in mustache .. 
make things more modular. We ahve to check that user is logged 
in a lot, should try to call taht using 'next() or something.
Look at data model. Maybe things could be simpler.
